<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfico HTTP cleartext solo a localhost y 10.0.2.2 (host del emulador AVD).
  Necesario porque el backend corre en http://localhost:3000 durante desarrollo.
  Con `adb reverse tcp:3000 tcp:3000`, el dispositivo accede a localhost:3000
  y la solicitud se redirige al puerto 3000 del host Ubuntu.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- 10.0.2.2 es el host desde el emulador AVD (sin adb reverse) -->
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
